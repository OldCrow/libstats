// Auto-generated by scripts/gen_trig_cleanroom_table.py -- do not edit.
// Clean-room derived constants for the x86 vector_cos_<tier>/
// vector_sin_<tier> kernels (issue #95); bit-identical to the
// kTrigNeon* constants in src/neon_trig_cleanroom_data.inc (verified by
// this script's self-check). See docs/NEON_TRIG_DERIVATION.md and
// docs/NEON_TRIG_DIVERGENCE_AUDIT.md for the mathematics and the
// point-by-point comparison against ARM optimized-routines advsimd
// sin.c/cos.c confirming no shared expression. No third-party source.

static constexpr double kTrigDMax = 0x1.0000000000000p+23;  // 2^23
static constexpr double kTrigTwoOverPi = 0x1.45f306dc9c883p-1;

// pi/2 split into exact-product parts: 30/30/30 significant bits + full
// tail; n*part exact for |n| < 2^23. Subtract in order with FMS.
static constexpr double kTrigPio2[4] = {
    0x1.921fb54800000p+0,
    -0x1.de973dc800000p-31,
    -0x1.9d9cceb800000p-62,
    -0x1.1fc8f8cbb5bf7p-93,
};

// degree-6 near-minimax cores on u = r^2 in [0, (pi/4)^2 (padded)]:
// sin(r) = r + r*(u*P(u)); cos(r) = 1 + u*Q(u), Q[0] == -1/2 exactly
// (required by the kernel's exact 1 - u/2 head/tail split).
static constexpr double kTrigSinC[7] = {
    -0x1.5555555555555p-3,
    0x1.1111111111110p-7,
    -0x1.a01a01a019938p-13,
    0x1.71de3a5460952p-19,
    -0x1.ae645412c46d3p-26,
    0x1.61217f0abf087p-33,
    -0x1.ab17d393166dcp-41,
};
static constexpr double kTrigCosC[7] = {
    -0x1.0000000000000p-1,
    0x1.5555555555551p-5,
    -0x1.6c16c16c15d79p-10,
    0x1.a01a019de130cp-16,
    -0x1.27e4f8e4a1e4cp-22,
    0x1.1eea7f24ce4e2p-29,
    -0x1.8ff9d3c0d9835p-37,
};
